# HYDRA v2 Phase 2: 서버 API 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 API를 멀티머신 태스크 기반으로 확장하고, 프리셋 관리/주간 목표/영상 자동 수집/WebSocket 실시간 업데이트를 추가한다.

**Architecture:** 기존 hydra/web/routes/ 엔드포인트는 유지하면서, 캠페인 생성 시 Task 테이블에 태스크를 자동 분해하는 로직 추가. WebSocket으로 실시간 상태 전파. 기존 background job을 태스크 기반으로 전환.

**Tech Stack:** FastAPI, SQLAlchemy, WebSocket, APScheduler

**Spec Reference:** `docs/superpowers/specs/2026-04-16-core-engine-design.md`

---

## File Structure

```
hydra/
├── api/
│   ├── presets.py              # CREATE: 프리셋 CRUD
│   └── websocket.py            # CREATE: WebSocket 엔드포인트
├── services/
│   ├── campaign_service.py     # CREATE: 캠페인 → 태스크 분해
│   ├── preset_service.py       # CREATE: 프리셋 CRUD 로직
│   ├── video_collector.py      # CREATE: 영상 자동 수집
│   └── realtime.py             # CREATE: WebSocket 매니저
├── web/routes/
│   ├── brands.py               # MODIFY: 주간 목표 필드 추가
│   ├── campaigns.py            # MODIFY: 태스크 기반 캠페인 생성
│   ├── dashboard.py            # MODIFY: 워커 상태 포함
│   └── settings.py             # MODIFY: 프리셋 API 연결
├── db/
│   └── models.py               # MODIFY: Brand에 주간 목표 컬럼 추가
tests/
├── test_preset_api.py          # CREATE
├── test_campaign_service.py    # CREATE
└── test_websocket.py           # CREATE
```

---

### Task 1: 프리셋 CRUD API

**Files:**
- Create: `hydra/services/preset_service.py`
- Create: `hydra/api/presets.py`
- Create: `tests/test_preset_api.py`
- Modify: `hydra/web/app.py`

- [ ] **Step 1: preset_service.py 작성**

```python
import json
from sqlalchemy.orm import Session
from hydra.db.models import Preset

def list_presets(db: Session, include_system: bool = True) -> list[Preset]:
    query = db.query(Preset)
    if not include_system:
        query = query.filter(Preset.is_system == False)
    return query.order_by(Preset.code).all()

def get_preset(db: Session, preset_id: int) -> Preset | None:
    return db.get(Preset, preset_id)

def get_preset_by_code(db: Session, code: str) -> Preset | None:
    return db.query(Preset).filter(Preset.code == code).first()

def create_preset(db: Session, name: str, code: str, description: str, steps: list[dict]) -> Preset:
    preset = Preset(
        name=name,
        code=code,
        is_system=False,
        description=description,
        steps=json.dumps(steps, ensure_ascii=False),
    )
    db.add(preset)
    db.commit()
    db.refresh(preset)
    return preset

def update_preset(db: Session, preset_id: int, data: dict) -> Preset | None:
    preset = db.get(Preset, preset_id)
    if not preset:
        return None
    if "name" in data:
        preset.name = data["name"]
    if "description" in data:
        preset.description = data["description"]
    if "steps" in data:
        preset.steps = json.dumps(data["steps"], ensure_ascii=False)
    db.commit()
    db.refresh(preset)
    return preset

def delete_preset(db: Session, preset_id: int) -> bool:
    preset = db.get(Preset, preset_id)
    if not preset or preset.is_system:
        return False
    db.delete(preset)
    db.commit()
    return True
```

- [ ] **Step 2: presets.py API 엔드포인트 작성**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from pydantic import BaseModel
from hydra.db.session import get_db
from hydra.services import preset_service
import json

router = APIRouter(prefix="/api/presets", tags=["presets"])

class PresetCreate(BaseModel):
    name: str
    code: str
    description: str = ""
    steps: list[dict]

class PresetUpdate(BaseModel):
    name: str | None = None
    description: str | None = None
    steps: list[dict] | None = None

@router.get("/")
def list_presets(db: Session = Depends(get_db)):
    presets = preset_service.list_presets(db)
    return [
        {
            "id": p.id, "name": p.name, "code": p.code,
            "is_system": p.is_system, "description": p.description,
            "steps": json.loads(p.steps), "step_count": len(json.loads(p.steps)),
        }
        for p in presets
    ]

@router.get("/{preset_id}")
def get_preset(preset_id: int, db: Session = Depends(get_db)):
    preset = preset_service.get_preset(db, preset_id)
    if not preset:
        raise HTTPException(status_code=404, detail="Preset not found")
    return {
        "id": preset.id, "name": preset.name, "code": preset.code,
        "is_system": preset.is_system, "description": preset.description,
        "steps": json.loads(preset.steps),
    }

@router.post("/")
def create_preset(body: PresetCreate, db: Session = Depends(get_db)):
    existing = preset_service.get_preset_by_code(db, body.code)
    if existing:
        raise HTTPException(status_code=409, detail=f"Code '{body.code}' already exists")
    preset = preset_service.create_preset(db, body.name, body.code, body.description, body.steps)
    return {"id": preset.id, "name": preset.name, "code": preset.code}

@router.put("/{preset_id}")
def update_preset(preset_id: int, body: PresetUpdate, db: Session = Depends(get_db)):
    data = body.model_dump(exclude_none=True)
    preset = preset_service.update_preset(db, preset_id, data)
    if not preset:
        raise HTTPException(status_code=404, detail="Preset not found")
    return {"id": preset.id, "name": preset.name}

@router.delete("/{preset_id}")
def delete_preset(preset_id: int, db: Session = Depends(get_db)):
    if not preset_service.delete_preset(db, preset_id):
        raise HTTPException(status_code=400, detail="Cannot delete (not found or system preset)")
    return {"ok": True}
```

- [ ] **Step 3: app.py에 프리셋 라우터 등록**

- [ ] **Step 4: 테스트 작성 (tests/test_preset_api.py)**

CRUD 전체 테스트: 목록조회, 생성, 수정, 삭제, 시스템 프리셋 삭제 실패, 중복 코드 거부

- [ ] **Step 5: 테스트 실행 및 커밋**

---

### Task 2: 캠페인 생성 → 태스크 자동 분해

**Files:**
- Create: `hydra/services/campaign_service.py`
- Create: `tests/test_campaign_service.py`
- Modify: `hydra/web/routes/campaigns.py`

- [ ] **Step 1: campaign_service.py — 캠페인 생성 시 태스크 분해 로직**

```python
import json
import random
from datetime import datetime, timedelta, UTC
from sqlalchemy.orm import Session
from hydra.db.models import Campaign, CampaignStep, Task, Preset, Account, Video

def create_campaign_with_tasks(
    db: Session,
    video_id: str,
    brand_id: int,
    preset_code: str,
    campaign_type: str = "scenario",
    comment_mode: str = "ai_auto",
) -> Campaign:
    """캠페인 생성 + 프리셋 기반 태스크 자동 분해."""
    preset = db.query(Preset).filter(Preset.code == preset_code).first()
    if not preset:
        raise ValueError(f"Preset '{preset_code}' not found")

    # 캠페인 생성
    campaign = Campaign(
        video_id=video_id,
        brand_id=brand_id,
        scenario=preset_code,
        campaign_type=campaign_type,
        comment_mode=comment_mode,
        preset_id=preset.id,
        status="planning",
    )
    db.add(campaign)
    db.flush()

    # 프리셋 스텝 → 태스크 분해
    steps = json.loads(preset.steps)
    base_time = datetime.now(UTC)

    for step in steps:
        delay_min = step.get("delay_min", 0)
        delay_max = step.get("delay_max", 0)
        delay = random.uniform(delay_min, delay_max) if delay_max > 0 else 0
        scheduled_at = base_time + timedelta(minutes=delay)

        # 메인 댓글/대댓글 태스크
        task = Task(
            campaign_id=campaign.id,
            task_type=step["type"],
            priority="normal",
            status="pending",
            payload=json.dumps({
                "step_number": step["step_number"],
                "role": step["role"],
                "tone": step.get("tone", ""),
                "target": step.get("target", "main"),
                "video_id": video_id,
                "brand_id": brand_id,
                "preset_code": preset_code,
            }, ensure_ascii=False),
            scheduled_at=scheduled_at,
        )
        db.add(task)

        # 좋아요 부스트 태스크
        like_count = step.get("like_count", 0)
        if like_count > 0:
            like_delay = delay + random.uniform(2, 8)  # 댓글 후 2~8분
            for i in range(like_count):
                like_scheduled = base_time + timedelta(
                    minutes=like_delay + random.uniform(0.25, 1.5) * i  # 15~90초 간격
                )
                like_task = Task(
                    campaign_id=campaign.id,
                    task_type="like_boost",
                    priority="low",
                    status="pending",
                    payload=json.dumps({
                        "target_step": step["step_number"],
                        "video_id": video_id,
                    }, ensure_ascii=False),
                    scheduled_at=like_scheduled,
                )
                db.add(like_task)

    campaign.status = "in_progress"
    db.commit()
    db.refresh(campaign)
    return campaign

def create_direct_campaign(
    db: Session,
    video_urls: list[str],
    actions: dict,
) -> list[Campaign]:
    """다이렉트 캠페인: URL 여러 개 + 작업 선택."""
    campaigns = []
    for url in video_urls:
        # URL에서 video_id 추출
        video_id = _extract_video_id(url)
        if not video_id:
            continue

        campaign = Campaign(
            video_id=video_id,
            campaign_type="direct",
            comment_mode=actions.get("comment_mode", "manual"),
            status="in_progress",
        )
        db.add(campaign)
        db.flush()

        base_time = datetime.now(UTC)

        # 좋아요 태스크
        like_count = actions.get("like_count", 0)
        for i in range(like_count):
            task = Task(
                campaign_id=campaign.id,
                task_type="like",
                priority="normal",
                status="pending",
                payload=json.dumps({"video_id": video_id}, ensure_ascii=False),
                scheduled_at=base_time + timedelta(seconds=random.uniform(15, 90) * i),
            )
            db.add(task)

        # 댓글 태스크
        comments = actions.get("comments", [])
        for j, comment in enumerate(comments):
            task = Task(
                campaign_id=campaign.id,
                task_type="comment",
                priority="normal",
                status="pending",
                payload=json.dumps({
                    "video_id": video_id,
                    "text": comment.get("text", ""),
                    "mode": comment.get("mode", "manual"),
                }, ensure_ascii=False),
                scheduled_at=base_time + timedelta(minutes=random.uniform(3, 15) * j),
            )
            db.add(task)

        # 구독 태스크
        if actions.get("subscribe", False):
            task = Task(
                campaign_id=campaign.id,
                task_type="subscribe",
                priority="low",
                status="pending",
                payload=json.dumps({"video_id": video_id}, ensure_ascii=False),
                scheduled_at=base_time,
            )
            db.add(task)

        db.commit()
        campaigns.append(campaign)

    return campaigns

def _extract_video_id(url: str) -> str | None:
    """YouTube URL에서 video ID 추출."""
    import re
    patterns = [
        r'(?:v=|/v/|youtu\.be/)([a-zA-Z0-9_-]{11})',
        r'(?:shorts/)([a-zA-Z0-9_-]{11})',
    ]
    for pattern in patterns:
        match = re.search(pattern, url)
        if match:
            return match.group(1)
    return None
```

- [ ] **Step 2: 테스트 작성 (tests/test_campaign_service.py)**

캠페인 생성 → 태스크 분해 검증: 프리셋 B (3 스텝 + 좋아요) → 태스크 수 확인, 타이밍 랜덤 확인, 다이렉트 캠페인 테스트

- [ ] **Step 3: 기존 campaigns.py에 태스크 기반 캠페인 생성 연결**

기존 `/api/create` 엔드포인트에서 `campaign_service.create_campaign_with_tasks()` 호출하도록 수정. 기존 로직은 유지하되 추가로 태스크도 생성.

- [ ] **Step 4: 테스트 실행 및 커밋**

---

### Task 3: 브랜드에 주간 목표 추가

**Files:**
- Modify: `hydra/db/models.py`
- Create: `alembic/versions/xxxx_brand_weekly_goal.py`
- Modify: `hydra/web/routes/brands.py`

- [ ] **Step 1: Brand 모델에 주간 목표 컬럼 추가**

```python
# Brand 모델에 추가:
weekly_campaign_target = Column(Integer, default=0)  # 주간 캠페인 목표 (0 = 비활성)
auto_campaign_enabled = Column(Boolean, default=False)  # 자동 캠페인 생성 활성화
```

- [ ] **Step 2: Alembic 마이그레이션 생성 및 실행**

- [ ] **Step 3: brands.py에 주간 목표 조회/수정 API 추가**

기존 `/api/{brand_id}/update` 에서 `weekly_campaign_target`, `auto_campaign_enabled` 필드 처리 추가.

- [ ] **Step 4: 커밋**

---

### Task 4: 영상 자동 수집 스케줄러

**Files:**
- Create: `hydra/services/video_collector.py`
- Modify: `hydra/web/routes/videos.py`

- [ ] **Step 1: video_collector.py — 키워드 기반 영상 수집**

```python
from datetime import datetime, UTC
from sqlalchemy.orm import Session
from hydra.db.models import Brand, Keyword, Video
from hydra.collection.youtube_api import YouTubeAPI

def collect_videos_for_brand(db: Session, brand_id: int, max_per_keyword: int = 10) -> list[Video]:
    """브랜드의 타겟 키워드로 YouTube 영상 수집."""
    brand = db.get(Brand, brand_id)
    if not brand:
        return []

    keywords = db.query(Keyword).filter(
        Keyword.brand_id == brand_id,
        Keyword.status == "active",
    ).all()

    collected = []
    api = YouTubeAPI()

    for kw in keywords:
        try:
            videos = api.search_videos(kw.text, max_results=max_per_keyword)
            for v_data in videos:
                # 이미 존재하는 영상 건너뜀
                existing = db.get(Video, v_data["id"])
                if existing:
                    continue

                video = Video(
                    id=v_data["id"],
                    url=f"https://www.youtube.com/watch?v={v_data['id']}",
                    title=v_data.get("title", ""),
                    channel_id=v_data.get("channel_id", ""),
                    channel_title=v_data.get("channel_title", ""),
                    view_count=v_data.get("view_count", 0),
                    comment_count=v_data.get("comment_count", 0),
                    published_at=v_data.get("published_at"),
                    is_short=v_data.get("is_short", False),
                    keyword_id=kw.id,
                    status="available",
                )
                db.add(video)
                collected.append(video)

            kw.last_searched_at = datetime.now(UTC)
            kw.total_videos_found += len(collected)
        except Exception:
            continue

    db.commit()
    return collected
```

- [ ] **Step 2: videos.py에 수동 수집 트리거 API 추가**

```python
@router.post("/api/collect")
def collect_videos(brand_id: int, db: Session = Depends(get_db)):
    from hydra.services.video_collector import collect_videos_for_brand
    videos = collect_videos_for_brand(db, brand_id)
    return {"ok": True, "collected": len(videos)}
```

- [ ] **Step 3: 커밋**

---

### Task 5: WebSocket 실시간 업데이트

**Files:**
- Create: `hydra/services/realtime.py`
- Create: `hydra/api/websocket.py`
- Create: `tests/test_websocket.py`
- Modify: `hydra/web/app.py`
- Modify: `hydra/services/task_service.py`

- [ ] **Step 1: realtime.py — 연결 매니저**

```python
from fastapi import WebSocket
import json

class ConnectionManager:
    def __init__(self):
        self.active_connections: list[WebSocket] = []

    async def connect(self, websocket: WebSocket):
        await websocket.accept()
        self.active_connections.append(websocket)

    def disconnect(self, websocket: WebSocket):
        self.active_connections.remove(websocket)

    async def broadcast(self, event_type: str, data: dict):
        message = json.dumps({"type": event_type, "data": data})
        dead = []
        for conn in self.active_connections:
            try:
                await conn.send_text(message)
            except Exception:
                dead.append(conn)
        for conn in dead:
            self.active_connections.remove(conn)

manager = ConnectionManager()
```

- [ ] **Step 2: websocket.py — WebSocket 엔드포인트**

```python
from fastapi import APIRouter, WebSocket, WebSocketDisconnect
from hydra.services.realtime import manager

router = APIRouter()

@router.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    await manager.connect(websocket)
    try:
        while True:
            await websocket.receive_text()  # keepalive
    except WebSocketDisconnect:
        manager.disconnect(websocket)
```

- [ ] **Step 3: task_service.py에 실시간 브로드캐스트 추가**

태스크 완료/실패 시 WebSocket으로 브로드캐스트:

```python
import asyncio
from hydra.services.realtime import manager

# complete_task 끝에:
try:
    asyncio.get_event_loop().create_task(
        manager.broadcast("task_completed", {"task_id": task.id, "campaign_id": task.campaign_id})
    )
except RuntimeError:
    pass  # 이벤트 루프 없으면 무시
```

- [ ] **Step 4: app.py에 WebSocket 라우터 등록**

- [ ] **Step 5: 테스트 및 커밋**

---

### Task 6: 대시보드 API 확장 (워커 상태 포함)

**Files:**
- Modify: `hydra/web/routes/dashboard.py`

- [ ] **Step 1: dashboard.py의 /api/stats에 워커 정보 추가**

기존 응답에 추가:

```python
from hydra.db.models import Worker, Task

# 워커 상태
workers = db.query(Worker).all()
worker_stats = {
    "online": sum(1 for w in workers if w.status == "online"),
    "total": len(workers),
}

# 오늘 태스크 통계
today_start = datetime.now(UTC).replace(hour=0, minute=0, second=0)
task_stats = {
    "today_completed": db.query(Task).filter(Task.completed_at >= today_start, Task.status == "completed").count(),
    "today_failed": db.query(Task).filter(Task.completed_at >= today_start, Task.status == "failed").count(),
    "pending": db.query(Task).filter(Task.status == "pending").count(),
    "running": db.query(Task).filter(Task.status.in_(["assigned", "running"])).count(),
}
```

- [ ] **Step 2: 커밋**

---

### Task 7: 전체 테스트 및 정리

- [ ] **Step 1: 전체 테스트 실행**

Run: `.venv/bin/python -m pytest tests/ -v`
Expected: 모든 테스트 PASSED

- [ ] **Step 2: 커밋**

```bash
git commit -am "feat: Phase 2 완료 — 서버 API (프리셋, 캠페인 태스크 분해, WebSocket, 영상 수집)"
```
