# M2.1 신호 루프 검증 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 실 Gmail/AdsPower/브라우저 없이 워커↔서버↔대시보드 신호 루프가 실시간으로 작동함을 증명한다. DRY-RUN 모드 + 실시간 대시보드 + 태스크 통계 API.

**Architecture:** `HYDRA_WORKER_DRY_RUN=1` 환경변수 하나로 executor 를 실 로직 우회 모드로 전환. 서버에 `/api/admin/tasks/stats` + `/api/admin/tasks/recent` 추가. 대시보드/워커/계정 페이지 polling 주기 단축 + 워커 현재 태스크 표시. 신규 DB 스키마 없음.

**Tech Stack:** Python 3.11 / FastAPI / SQLAlchemy / TanStack Router + shadcn/ui / 기존 workflow (git push → admin 배포 버튼)

**참조 Spec:** [`../specs/2026-04-23-m2.1-signal-loop-design.md`](../specs/2026-04-23-m2.1-signal-loop-design.md)

---

## File Structure

### 서버
- **Modify**: `worker/executor.py` — DRY-RUN 진입 가드
- **Create**: `hydra/web/routes/admin_tasks.py` — 통계/최근 조회 엔드포인트
- **Modify**: `hydra/web/routes/admin_workers.py` — list 응답에 `current_task` 추가
- **Modify**: `hydra/web/app.py` — admin_tasks 라우터 등록

### 프론트
- **Modify**: `frontend/src/features/workers/index.tsx` — polling 3초 + current_task 표시
- **Modify**: `frontend/src/features/accounts/index.tsx` — polling 10s → 3s
- **Modify**: `frontend/src/features/dashboard/index.tsx` — polling 30s → 10s
- **Create or Modify**: `frontend/src/features/tasks/stats-panel.tsx` — 통계 카드
- **Modify**: `frontend/src/routes/_authenticated/tasks/index.tsx` — stats 패널 통합

### 도구
- **Create**: `scripts/run_mac_worker_dry.sh` — Mac 워커 DRY-RUN 기동
- **Create**: `scripts/m2_1_loop_check.sh` — 신호 루프 자동 검증
- **Modify**: `scripts/e2e_check.sh` — 신규 엔드포인트 추가

### 테스트
- `tests/test_worker_dry_run.py` — executor DRY-RUN 분기
- `tests/test_admin_tasks_api.py` — stats/recent
- `tests/test_admin_workers_current_task.py` — list 응답 current_task

---

## 진행 방침

- 각 task 후 push → admin UI 배포 버튼 (또는 API) 으로 반영
- TDD — 서버 변경은 pytest, 프론트는 build 성공 + 수동 관찰
- main 직행, 체크포인트 1회 (M2.1-5 서버 완료 시점)

---

## Task M2.1-1: Worker DRY-RUN 모드

**Files:**
- Modify: `worker/executor.py`
- Test: `tests/test_worker_dry_run.py`

- [ ] **Step 1: 테스트 작성**

`tests/test_worker_dry_run.py`:
```python
"""Task M2.1-1: worker executor DRY-RUN 모드.

HYDRA_WORKER_DRY_RUN=1 env 가 설정되면 execute 가 실 로직 없이 즉시 성공을 리턴.
"""
import asyncio
from unittest.mock import patch

import pytest


def _import_executor_fresh(monkeypatch, dry_run: bool):
    """env 세팅 후 모듈 reload — _DRY_RUN 상수 재평가."""
    if dry_run:
        monkeypatch.setenv("HYDRA_WORKER_DRY_RUN", "1")
    else:
        monkeypatch.delenv("HYDRA_WORKER_DRY_RUN", raising=False)
    import importlib
    import worker.executor as ex
    importlib.reload(ex)
    return ex


def test_dry_run_flag_parsed_true(monkeypatch):
    ex = _import_executor_fresh(monkeypatch, dry_run=True)
    assert ex._DRY_RUN is True


def test_dry_run_flag_parsed_false(monkeypatch):
    ex = _import_executor_fresh(monkeypatch, dry_run=False)
    assert ex._DRY_RUN is False


def test_dry_run_flag_case_insensitive(monkeypatch):
    for v in ("1", "true", "TRUE", "yes", "Yes"):
        monkeypatch.setenv("HYDRA_WORKER_DRY_RUN", v)
        import importlib
        import worker.executor as ex
        importlib.reload(ex)
        assert ex._DRY_RUN is True, f"failed for {v!r}"
    # 빈 값/기타 → False
    for v in ("", "0", "false", "no", "abc"):
        monkeypatch.setenv("HYDRA_WORKER_DRY_RUN", v)
        import importlib
        import worker.executor as ex
        importlib.reload(ex)
        assert ex._DRY_RUN is False, f"should be false for {v!r}"


def test_execute_in_dry_run_returns_immediately_without_real_logic(monkeypatch):
    """DRY-RUN 이면 httpx/AdsPower/Google 호출 없이 즉시 done 리턴."""
    monkeypatch.setenv("HYDRA_WORKER_DRY_RUN", "1")
    import importlib
    import worker.executor as ex
    importlib.reload(ex)

    task = {
        "id": 1, "task_type": "warmup", "account_id": 42,
        "payload": "{}",
        "account_snapshot": {
            "id": 42, "gmail": "a@x.com",
            "adspower_profile_id": "p1", "encrypted_password": "",
        },
    }
    executor = ex.TaskExecutor()
    result = asyncio.run(executor.execute(task))
    assert result["ok"] is True
    assert result["dry_run"] is True
    assert result["task_type"] == "warmup"
```

- [ ] **Step 2: 실행 → FAIL**

```bash
.venv/bin/pytest tests/test_worker_dry_run.py -v
```
Expected: `AttributeError: module has no attribute '_DRY_RUN'`

- [ ] **Step 3: executor.py 수정**

`worker/executor.py` 최상단 import 섹션에 추가:

```python
import os as _os


def _parse_dry_run_flag() -> bool:
    return _os.getenv("HYDRA_WORKER_DRY_RUN", "").strip().lower() in (
        "1", "true", "yes",
    )


_DRY_RUN = _parse_dry_run_flag()
```

`TaskExecutor.execute` (메서드 이름/시그니처 기존 그대로) 본문 **최상단** 에 삽입:

```python
async def execute(self, task: dict) -> dict:
    # M2.1-1: DRY-RUN 모드 — 실 로직 건너뛰고 즉시 성공
    if _DRY_RUN:
        import asyncio
        await asyncio.sleep(0.5)  # 대시보드에서 running 상태 관찰용
        return {
            "ok": True,
            "dry_run": True,
            "task_type": task.get("task_type"),
        }
    # 기존 로직 이어짐
    ...
```

(`execute` 가 동기 메서드면 sleep 없이 바로 리턴. 기존 시그니처 확인 후 매칭.)

- [ ] **Step 4: 실행 → PASS**

```bash
.venv/bin/pytest tests/test_worker_dry_run.py -v
```

- [ ] **Step 5: 전체 회귀**

```bash
.venv/bin/pytest tests/ --ignore=tests/test_language_setup.py -q
```
기존 회귀 PASS 유지.

- [ ] **Step 6: Commit**

```bash
git add worker/executor.py tests/test_worker_dry_run.py
git commit -m "feat(worker): M2.1-1 HYDRA_WORKER_DRY_RUN executor 진입 가드"
```

---

## Task M2.1-2: Mac 워커 DRY-RUN 기동 스크립트

**Files:**
- Create: `scripts/run_mac_worker_dry.sh`

- [ ] **Step 1: 스크립트 작성**

`scripts/run_mac_worker_dry.sh`:
```bash
#!/usr/bin/env bash
# M2.1 — Mac 로컬 워커를 DRY-RUN 모드로 VPS 에 연결.
# 실 Gmail/AdsPower/브라우저 액션 없이 heartbeat/fetch/complete 루프만.
set -euo pipefail

cd "$(dirname "$0")/.."

SERVER_URL="${SERVER_URL:-https://hydra-prod.duckdns.org}"
ADMIN_EMAIL="${ADMIN_EMAIL:?ADMIN_EMAIL required}"
ADMIN_PASSWORD="${ADMIN_PASSWORD:?ADMIN_PASSWORD required}"

echo "[1/4] admin login"
ADMIN_TOKEN=$(curl -s -X POST "$SERVER_URL/api/admin/auth/login" \
    -H 'Content-Type: application/json' \
    -d "{\"email\":\"$ADMIN_EMAIL\",\"password\":\"$ADMIN_PASSWORD\"}" \
    | python3 -c 'import json,sys;print(json.load(sys.stdin)["token"])')

echo "[2/4] enrollment token"
ENROLL=$(curl -s -X POST "$SERVER_URL/api/admin/workers/enroll" \
    -H "Authorization: Bearer $ADMIN_TOKEN" \
    -H 'Content-Type: application/json' \
    -d '{"worker_name":"mac-dryrun","ttl_hours":1}' \
    | python3 -c 'import json,sys;print(json.load(sys.stdin)["enrollment_token"])')

echo "[3/4] worker enroll"
WT=$(python3 -c "
import json, urllib.request
body=json.dumps({'enrollment_token':'$ENROLL','hostname':'mac-dryrun'}).encode()
req=urllib.request.Request('$SERVER_URL/api/workers/enroll', data=body, method='POST',
    headers={'Content-Type':'application/json'})
print(json.load(urllib.request.urlopen(req))['worker_token'])
")

# .env 에 WORKER_TOKEN 주입 (기존 행 제거 후 추가)
grep -v '^WORKER_TOKEN=' .env 2>/dev/null > .env.tmp || true
mv .env.tmp .env 2>/dev/null || touch .env
echo "WORKER_TOKEN=$WT" >> .env
echo "HYDRA_WORKER_DRY_RUN=1" >> .env
grep -v '^HYDRA_WORKER_DRY_RUN=' .env | awk '!seen[$0]++' > .env.tmp
mv .env.tmp .env

echo "[4/4] launching worker (Ctrl+C to stop)"
export HYDRA_WORKER_DRY_RUN=1
export SERVER_URL="$SERVER_URL"
export WORKER_TOKEN="$WT"
.venv/bin/python -m worker
```

- [ ] **Step 2: 실행 권한**

```bash
chmod +x scripts/run_mac_worker_dry.sh
```

- [ ] **Step 3: Commit**

```bash
git add scripts/run_mac_worker_dry.sh
git commit -m "feat(scripts): M2.1-2 Mac 워커 DRY-RUN 기동 스크립트"
```

---

## Task M2.1-3: `/api/admin/tasks/stats` 엔드포인트

**Files:**
- Create: `hydra/web/routes/admin_tasks.py`
- Modify: `hydra/web/app.py`
- Test: `tests/test_admin_tasks_api.py`

- [ ] **Step 1: 테스트 작성**

`tests/test_admin_tasks_api.py`:
```python
"""Task M2.1-3/4: /api/admin/tasks/{stats,recent}."""
from datetime import UTC, datetime, timedelta

import jwt as _jwt
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

import hydra.db.session as session_mod
from hydra.db.models import Account, Base, Task, Worker


@pytest.fixture
def env(monkeypatch):
    engine = create_engine(
        "sqlite:///:memory:",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(engine)
    TestSession = sessionmaker(bind=engine, autocommit=False, autoflush=False)
    monkeypatch.setattr(session_mod, "engine", engine)
    monkeypatch.setattr(session_mod, "SessionLocal", TestSession)
    monkeypatch.setenv("JWT_SECRET", "test-jwt-secret-123456789")

    from hydra.web.app import app
    client = TestClient(app)
    now = datetime.now(UTC)
    token = _jwt.encode(
        {"user_id": 1, "role": "admin", "iat": now, "exp": now + timedelta(hours=1)},
        "test-jwt-secret-123456789", algorithm="HS256",
    )
    yield {"client": client, "token": token, "session": TestSession}
    engine.dispose()


def _hdr(env):
    return {"Authorization": f"Bearer {env['token']}"}


def test_stats_requires_auth(env):
    r = env["client"].get("/api/admin/tasks/stats")
    assert r.status_code == 401


def test_stats_returns_empty_counts_when_no_tasks(env):
    r = env["client"].get("/api/admin/tasks/stats", headers=_hdr(env))
    assert r.status_code == 200
    body = r.json()
    assert body["pending"] == 0
    assert body["running"] == 0
    assert body["done"] == 0
    assert body["failed"] == 0
    assert body["by_type"] == {}


def test_stats_aggregates_by_status_and_type(env):
    db = env["session"]()
    acc = Account(gmail="a@x.com", password="x", adspower_profile_id="p1",
                  status="active")
    db.add(acc); db.flush()
    db.add_all([
        Task(account_id=acc.id, task_type="warmup", status="pending"),
        Task(account_id=acc.id, task_type="warmup", status="pending"),
        Task(account_id=acc.id, task_type="warmup", status="done"),
        Task(account_id=acc.id, task_type="comment", status="running"),
        Task(account_id=acc.id, task_type="comment", status="failed"),
    ])
    db.commit()
    db.close()

    r = env["client"].get("/api/admin/tasks/stats", headers=_hdr(env))
    body = r.json()
    assert body["pending"] == 2
    assert body["running"] == 1
    assert body["done"] == 1
    assert body["failed"] == 1
    assert body["by_type"]["warmup"] == {
        "pending": 2, "running": 0, "done": 1, "failed": 0,
    }
    assert body["by_type"]["comment"] == {
        "pending": 0, "running": 1, "done": 0, "failed": 1,
    }
```

- [ ] **Step 2: 실행 → FAIL**

```bash
.venv/bin/pytest tests/test_admin_tasks_api.py -v -k "stats"
```

- [ ] **Step 3: `admin_tasks.py` 생성**

`hydra/web/routes/admin_tasks.py`:
```python
"""Task M2.1-3/4: 어드민 태스크 통계 + 최근 목록 조회."""
from __future__ import annotations

from datetime import datetime
from typing import Optional

from fastapi import APIRouter, Depends, Query
from pydantic import BaseModel
from sqlalchemy import func

from hydra.db import session as _db_session
from hydra.db.models import Account, Task, Worker
from hydra.web.routes.admin_auth import admin_session

router = APIRouter()

_STATUSES = ("pending", "running", "done", "failed")


class TasksStatsResponse(BaseModel):
    pending: int
    running: int
    done: int
    failed: int
    by_type: dict


@router.get("/stats", response_model=TasksStatsResponse)
def stats(_session: dict = Depends(admin_session)) -> TasksStatsResponse:
    db = _db_session.SessionLocal()
    try:
        rows = (
            db.query(Task.task_type, Task.status, func.count(Task.id))
            .group_by(Task.task_type, Task.status)
            .all()
        )
        totals = {s: 0 for s in _STATUSES}
        by_type: dict[str, dict[str, int]] = {}
        for task_type, status, count in rows:
            if status not in _STATUSES:
                continue
            totals[status] = totals.get(status, 0) + count
            by_type.setdefault(task_type, {s: 0 for s in _STATUSES})[status] = count
        return TasksStatsResponse(**totals, by_type=by_type)
    finally:
        db.close()
```

- [ ] **Step 4: `app.py` 에 라우터 등록**

`hydra/web/app.py` 상단 import 에 `admin_tasks` 추가, `admin_accounts.router` 등록 다음 줄에:

```python
app.include_router(admin_tasks.router, prefix="/api/admin/tasks",
                   tags=["admin-tasks"], dependencies=_ADMIN_DEPS)
```

- [ ] **Step 5: 실행 → PASS**

```bash
.venv/bin/pytest tests/test_admin_tasks_api.py -v -k "stats"
```

- [ ] **Step 6: Commit**

```bash
git add hydra/web/routes/admin_tasks.py hydra/web/app.py tests/test_admin_tasks_api.py
git commit -m "feat(admin): M2.1-3 GET /api/admin/tasks/stats"
```

---

## Task M2.1-4: `/api/admin/tasks/recent` 엔드포인트

**Files:**
- Modify: `hydra/web/routes/admin_tasks.py`
- Modify: `tests/test_admin_tasks_api.py`

- [ ] **Step 1: 테스트 추가**

`tests/test_admin_tasks_api.py` 끝에:
```python
def test_recent_requires_auth(env):
    r = env["client"].get("/api/admin/tasks/recent")
    assert r.status_code == 401


def test_recent_returns_latest_first_with_account_and_worker(env):
    db = env["session"]()
    w = Worker(name="w1", token_hash="h")
    acc = Account(gmail="u@x.com", password="x", adspower_profile_id="p1",
                  status="active")
    db.add_all([w, acc]); db.flush()
    db.add_all([
        Task(account_id=acc.id, worker_id=w.id, task_type="warmup", status="done"),
        Task(account_id=acc.id, worker_id=w.id, task_type="comment", status="pending"),
    ])
    db.commit()
    db.close()

    r = env["client"].get("/api/admin/tasks/recent?limit=10", headers=_hdr(env))
    assert r.status_code == 200
    items = r.json()["items"]
    assert len(items) == 2
    # 최신순 (id 내림차순)
    assert items[0]["id"] > items[1]["id"]
    row = items[0]
    assert row["task_type"] == "comment"
    assert row["status"] == "pending"
    assert row["account_gmail"] == "u@x.com"
    assert row["worker_name"] == "w1"


def test_recent_limit_enforced(env):
    db = env["session"]()
    acc = Account(gmail="u@x.com", password="x", adspower_profile_id="p1",
                  status="active")
    db.add(acc); db.flush()
    for i in range(25):
        db.add(Task(account_id=acc.id, task_type="warmup", status="pending"))
    db.commit()
    db.close()

    r = env["client"].get("/api/admin/tasks/recent?limit=5", headers=_hdr(env))
    assert len(r.json()["items"]) == 5
```

- [ ] **Step 2: 실행 → FAIL**

- [ ] **Step 3: admin_tasks 확장**

`hydra/web/routes/admin_tasks.py` 에 추가:
```python
class RecentTaskItem(BaseModel):
    id: int
    task_type: str
    status: str
    account_id: Optional[int]
    account_gmail: Optional[str]
    worker_id: Optional[int]
    worker_name: Optional[str]
    created_at: Optional[datetime]
    started_at: Optional[datetime]
    completed_at: Optional[datetime]


class RecentTasksResponse(BaseModel):
    items: list[RecentTaskItem]


@router.get("/recent", response_model=RecentTasksResponse)
def recent(
    _session: dict = Depends(admin_session),
    limit: int = Query(20, ge=1, le=200),
) -> RecentTasksResponse:
    db = _db_session.SessionLocal()
    try:
        rows = (
            db.query(Task, Account.gmail, Worker.name)
            .outerjoin(Account, Task.account_id == Account.id)
            .outerjoin(Worker, Task.worker_id == Worker.id)
            .order_by(Task.id.desc())
            .limit(limit)
            .all()
        )
        items = [
            RecentTaskItem(
                id=t.id, task_type=t.task_type, status=t.status,
                account_id=t.account_id, account_gmail=gmail,
                worker_id=t.worker_id, worker_name=wname,
                created_at=t.created_at, started_at=t.started_at,
                completed_at=t.completed_at,
            )
            for (t, gmail, wname) in rows
        ]
        return RecentTasksResponse(items=items)
    finally:
        db.close()
```

- [ ] **Step 4: 실행 → PASS**

```bash
.venv/bin/pytest tests/test_admin_tasks_api.py -v
```

- [ ] **Step 5: Commit**

```bash
git add hydra/web/routes/admin_tasks.py tests/test_admin_tasks_api.py
git commit -m "feat(admin): M2.1-4 GET /api/admin/tasks/recent"
```

---

## Task M2.1-5: `/api/admin/workers/` 응답에 current_task 추가

**Files:**
- Modify: `hydra/web/routes/admin_workers.py`
- Test: `tests/test_admin_workers_current_task.py`

- [ ] **Step 1: 테스트 작성**

`tests/test_admin_workers_current_task.py`:
```python
"""Task M2.1-5: /api/admin/workers/ 응답에 current_task 포함."""
from datetime import UTC, datetime, timedelta

import jwt as _jwt
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

import hydra.db.session as session_mod
from hydra.db.models import Account, Base, Task, Worker


@pytest.fixture
def env(monkeypatch):
    engine = create_engine(
        "sqlite:///:memory:",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(engine)
    TestSession = sessionmaker(bind=engine, autocommit=False, autoflush=False)
    monkeypatch.setattr(session_mod, "engine", engine)
    monkeypatch.setattr(session_mod, "SessionLocal", TestSession)
    monkeypatch.setenv("JWT_SECRET", "test-jwt-secret-123456789")

    from hydra.web.app import app
    client = TestClient(app)
    now = datetime.now(UTC)
    token = _jwt.encode(
        {"user_id": 1, "role": "admin", "iat": now, "exp": now + timedelta(hours=1)},
        "test-jwt-secret-123456789", algorithm="HS256",
    )
    yield {"client": client, "token": token, "session": TestSession}
    engine.dispose()


def _hdr(env):
    return {"Authorization": f"Bearer {env['token']}"}


def test_worker_list_current_task_null_when_idle(env):
    db = env["session"]()
    db.add(Worker(name="idle", token_hash="h"))
    db.commit()
    db.close()

    r = env["client"].get("/api/admin/workers/", headers=_hdr(env))
    assert r.status_code == 200
    body = r.json()
    assert body[0]["current_task"] is None


def test_worker_list_current_task_filled_when_running(env):
    db = env["session"]()
    w = Worker(name="busy", token_hash="h")
    acc = Account(gmail="u@x.com", password="x", adspower_profile_id="p",
                  status="warmup", warmup_day=1)
    db.add_all([w, acc]); db.flush()
    t = Task(account_id=acc.id, worker_id=w.id, task_type="warmup",
             status="running", started_at=datetime.now(UTC))
    db.add(t); db.commit()
    db.close()

    r = env["client"].get("/api/admin/workers/", headers=_hdr(env))
    body = r.json()
    found = next(w for w in body if w["name"] == "busy")
    assert found["current_task"] is not None
    assert found["current_task"]["task_type"] == "warmup"
    assert found["current_task"]["id"] > 0
```

- [ ] **Step 2: 실행 → FAIL**

```bash
.venv/bin/pytest tests/test_admin_workers_current_task.py -v
```

- [ ] **Step 3: admin_workers.py 수정**

`WorkerOut` 모델에 필드 추가 + `_worker_to_out` 에서 running task 조회:

```python
class CurrentTaskInfo(BaseModel):
    id: int
    task_type: str
    started_at: Optional[datetime] = None


class WorkerOut(BaseModel):
    # ... 기존 필드 ...
    current_task: Optional[CurrentTaskInfo] = None


def _worker_to_out(w: Worker, current_task: Optional[Task] = None) -> WorkerOut:
    # ... 기존 세팅 ...
    ct = None
    if current_task is not None:
        ct = CurrentTaskInfo(
            id=current_task.id,
            task_type=current_task.task_type,
            started_at=current_task.started_at,
        )
    return WorkerOut(
        # ... 기존 인자 ...
        current_task=ct,
    )
```

**list_workers 함수 수정**:
```python
@router.get("/", response_model=list[WorkerOut])
def list_workers(_session: dict = Depends(admin_session)) -> list[WorkerOut]:
    db = _db_session.SessionLocal()
    try:
        workers = db.query(Worker).order_by(Worker.id).all()
        # 워커별 현재 running task 1건 조회 (N+1 이지만 워커 수 적어 OK)
        from hydra.db.models import Task
        result = []
        for w in workers:
            running = (
                db.query(Task)
                .filter(Task.worker_id == w.id, Task.status == "running")
                .first()
            )
            result.append(_worker_to_out(w, running))
        return result
    finally:
        db.close()
```

기존 `_worker_to_out` 호출부를 전부 `_worker_to_out(w, None)` 또는 Running task 포함으로 업데이트 — PATCH endpoint 에서도 current_task=None 으로 전달.

- [ ] **Step 4: 실행 → PASS**

```bash
.venv/bin/pytest tests/test_admin_workers_current_task.py -v
.venv/bin/pytest tests/test_admin_workers_patch.py -v  # 기존 회귀
```

- [ ] **Step 5: Commit**

```bash
git add hydra/web/routes/admin_workers.py tests/test_admin_workers_current_task.py
git commit -m "feat(admin-workers): M2.1-5 list 응답에 current_task 추가"
```

---

## 체크포인트: 서버 (M2.1-1~5) 완료 시점

- 전체 회귀 `pytest --ignore=tests/test_language_setup.py -q` 통과 확인
- `git push` + admin UI "배포" 버튼
- HTTPS 확인:
  - `curl -H "Authorization: Bearer $TOKEN" .../api/admin/tasks/stats` → 200
  - `curl -H "Authorization: Bearer $TOKEN" .../api/admin/tasks/recent` → 200
  - `curl -H "Authorization: Bearer $TOKEN" .../api/admin/workers/` → current_task 필드 존재
- Mac 에서 `bash scripts/run_mac_worker_dry.sh` → heartbeat 성공 로그

---

## Task M2.1-6: `/accounts` 페이지 polling 10s → 3s

**Files:**
- Modify: `frontend/src/features/accounts/index.tsx:189`

- [ ] **Step 1: 수정**

`frontend/src/features/accounts/index.tsx` 의 `setInterval` 호출 (~189 라인):

```tsx
// 기존:
const id = setInterval(() => load(false), 10_000)
// 변경:
const id = setInterval(() => load(false), 3_000)
```

- [ ] **Step 2: 빌드 확인**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd /Users/seominjae/Documents/hydra
git add frontend/src/features/accounts/index.tsx
git commit -m "feat(ui): M2.1-6 /accounts polling 3초로 단축"
```

---

## Task M2.1-7: `/workers` 페이지 3초 polling + current_task 표시

**Files:**
- Modify: `frontend/src/features/workers/index.tsx`

- [ ] **Step 1: Worker 타입 확장 + polling 추가**

`frontend/src/features/workers/index.tsx` 수정:

상단 Worker 인터페이스에 추가:
```tsx
interface Worker {
  // ... 기존 필드 ...
  current_task?: {
    id: number
    task_type: string
    started_at?: string | null
  } | null
}
```

`useEffect(() => { loadWorkers() }, [])` 블록을 polling 포함으로:
```tsx
useEffect(() => {
  loadWorkers()
  const id = setInterval(loadWorkers, 3_000)
  return () => clearInterval(id)
}, [])
```

워커 카드 안 "OS / 버전" 행 다음에 current_task 표시:
```tsx
<div className='flex justify-between'>
  <span className='text-muted-foreground'>현재 태스크</span>
  <span className='font-mono text-[12px]'>
    {worker.current_task
      ? `#${worker.current_task.id} · ${worker.current_task.task_type}`
      : '-'}
  </span>
</div>
```

- [ ] **Step 2: 빌드**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: Commit**

```bash
cd /Users/seominjae/Documents/hydra
git add frontend/src/features/workers/index.tsx
git commit -m "feat(ui): M2.1-7 /workers 3초 polling + current_task 표시"
```

---

## Task M2.1-8: `/tasks` 페이지 stats 패널

**Files:**
- Create: `frontend/src/features/tasks/stats-panel.tsx`
- Modify: `frontend/src/routes/_authenticated/tasks/index.tsx` (기존 구조 존중)

- [ ] **Step 1: 기존 tasks 페이지 구조 파악**

```bash
cat frontend/src/routes/_authenticated/tasks/index.tsx
ls frontend/src/features/tasks* 2>/dev/null
```

- [ ] **Step 2: stats-panel 작성**

`frontend/src/features/tasks/stats-panel.tsx`:
```tsx
import { useEffect, useState } from 'react'
import { Card } from '@/components/ui/card'
import { fetchApi } from '@/lib/api'

type Stats = {
  pending: number
  running: number
  done: number
  failed: number
  by_type: Record<string, { pending: number; running: number; done: number; failed: number }>
}

export function TaskStatsPanel() {
  const [stats, setStats] = useState<Stats | null>(null)

  useEffect(() => {
    const load = () => {
      fetchApi<Stats>('/api/admin/tasks/stats')
        .then(setStats)
        .catch(() => {})
    }
    load()
    const id = setInterval(load, 5_000)
    return () => clearInterval(id)
  }, [])

  if (!stats) return null

  return (
    <div className='space-y-3'>
      <div className='grid grid-cols-4 gap-3'>
        <Card className='p-3'>
          <div className='text-xs text-muted-foreground'>대기</div>
          <div className='text-2xl font-bold'>{stats.pending}</div>
        </Card>
        <Card className='p-3'>
          <div className='text-xs text-muted-foreground'>실행중</div>
          <div className='text-2xl font-bold text-amber-500'>{stats.running}</div>
        </Card>
        <Card className='p-3'>
          <div className='text-xs text-muted-foreground'>완료</div>
          <div className='text-2xl font-bold text-green-500'>{stats.done}</div>
        </Card>
        <Card className='p-3'>
          <div className='text-xs text-muted-foreground'>실패</div>
          <div className='text-2xl font-bold text-destructive'>{stats.failed}</div>
        </Card>
      </div>

      {Object.keys(stats.by_type).length > 0 && (
        <Card className='overflow-hidden'>
          <table className='w-full text-sm'>
            <thead className='bg-muted/40 text-xs uppercase text-muted-foreground'>
              <tr>
                <th className='px-3 py-2 text-left'>Task Type</th>
                <th className='px-3 py-2 text-right'>대기</th>
                <th className='px-3 py-2 text-right'>실행</th>
                <th className='px-3 py-2 text-right'>완료</th>
                <th className='px-3 py-2 text-right'>실패</th>
              </tr>
            </thead>
            <tbody>
              {Object.entries(stats.by_type).map(([type, counts]) => (
                <tr key={type} className='border-t'>
                  <td className='px-3 py-2 font-mono'>{type}</td>
                  <td className='px-3 py-2 text-right'>{counts.pending}</td>
                  <td className='px-3 py-2 text-right text-amber-500'>{counts.running}</td>
                  <td className='px-3 py-2 text-right text-green-500'>{counts.done}</td>
                  <td className='px-3 py-2 text-right text-destructive'>{counts.failed}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </Card>
      )}
    </div>
  )
}
```

- [ ] **Step 3: 기존 tasks 페이지에 삽입**

`frontend/src/routes/_authenticated/tasks/index.tsx` 또는 실제 tasks 메인 컴포넌트 파일에 TaskStatsPanel 을 페이지 최상단에 추가 (기존 컴포넌트 위). 페이지가 아예 없다면 신규 생성.

- [ ] **Step 4: 빌드**

```bash
cd frontend && npm run build
```

- [ ] **Step 5: Commit**

```bash
cd /Users/seominjae/Documents/hydra
git add frontend/src/features/tasks/ frontend/src/routes/_authenticated/tasks/
git commit -m "feat(ui): M2.1-8 /tasks stats 패널 (5초 polling)"
```

---

## Task M2.1-9: 대시보드 polling 30s → 10s

**Files:**
- Modify: `frontend/src/features/dashboard/index.tsx:75`

- [ ] **Step 1: 수정**

```tsx
// 기존:
const interval = setInterval(load, 30_000)
// 변경:
const interval = setInterval(load, 10_000)
```

- [ ] **Step 2: 빌드 + Commit**

```bash
cd frontend && npm run build
cd /Users/seominjae/Documents/hydra
git add frontend/src/features/dashboard/index.tsx
git commit -m "feat(ui): M2.1-9 대시보드 polling 10초로 단축"
```

---

## Task M2.1-10: E2E 루프 체크 스크립트

**Files:**
- Create: `scripts/m2_1_loop_check.sh`
- Modify: `scripts/e2e_check.sh`

- [ ] **Step 1: `m2_1_loop_check.sh` 작성**

`scripts/m2_1_loop_check.sh`:
```bash
#!/usr/bin/env bash
# M2.1-10: 어드민 UI 에서 등록된 계정이 DRY-RUN 워커로 active 까지 자동 전이하는지 확인.
# 전제: Mac 로컬 워커가 `scripts/run_mac_worker_dry.sh` 로 기동 중.
set -euo pipefail

URL="${HYDRA_URL:?HYDRA_URL required}"
EMAIL="${ADMIN_EMAIL:?ADMIN_EMAIL required}"
PW="${ADMIN_PASSWORD:?ADMIN_PASSWORD required}"

TOKEN=$(curl -s -X POST "$URL/api/admin/auth/login" \
    -H 'Content-Type: application/json' \
    -d "{\"email\":\"$EMAIL\",\"password\":\"$PW\"}" \
    | python3 -c 'import json,sys;print(json.load(sys.stdin)["token"])')

GMAIL="loopcheck-$(date +%s)@fake.local"
PROFILE="loopcheck-$(date +%s)"

echo "[register] $GMAIL / $PROFILE"
RESP=$(curl -s -X POST "$URL/api/admin/accounts/register" \
    -H "Authorization: Bearer $TOKEN" \
    -H 'Content-Type: application/json' \
    -d "{\"gmail\":\"$GMAIL\",\"password\":\"p\",\"adspower_profile_id\":\"$PROFILE\"}")
ACC_ID=$(echo "$RESP" | python3 -c 'import json,sys;print(json.load(sys.stdin)["account_id"])')
echo "  account_id=$ACC_ID"

DEADLINE=$(($(date +%s) + 60))
while [[ $(date +%s) -lt $DEADLINE ]]; do
    STATUS=$(curl -s "$URL/api/admin/tasks/recent?limit=100" \
        -H "Authorization: Bearer $TOKEN" \
        | python3 -c "
import json, sys
items = json.load(sys.stdin)['items']
states = [it['status'] for it in items if it['account_id'] == $ACC_ID]
print(','.join(states) if states else '-')
")
    echo "  tasks states: $STATUS"
    # Account 상태 조회
    ACC_STATE=$(curl -s "$URL/api/admin/tasks/stats" \
        -H "Authorization: Bearer $TOKEN" > /dev/null; echo polled)
    sleep 5
    # active 로 전이됐는지 간접 체크 — by_type done 카운트에 comment + like 존재
done

# 최종 DB 확인은 SSH 로
echo "done — check dashboard for final Account status"
```

- [ ] **Step 2: `e2e_check.sh` 에 신규 엔드포인트 추가**

`scripts/e2e_check.sh` 의 "_section "8. 감사 로그"" 전에:

```bash
# ── 9. 태스크 통계/최근 ──
_section "9. 태스크 통계 + 최근"
_expect_status 200 "$URL/api/admin/tasks/stats" GET "tasks/stats" "-H 'Authorization: Bearer $TOKEN'"
_expect_status 200 "$URL/api/admin/tasks/recent?limit=5" GET "tasks/recent" "-H 'Authorization: Bearer $TOKEN'"
```

주의: `_expect_status` 가 헤더 포함 지원하는지 확인. 안 되면 간단 curl 로 200 체크.

- [ ] **Step 3: 실행 권한 + 커밋**

```bash
chmod +x scripts/m2_1_loop_check.sh
git add scripts/m2_1_loop_check.sh scripts/e2e_check.sh
git commit -m "feat(scripts): M2.1-10 신호 루프 자동 검증 + e2e 확장"
```

---

## Task M2.1-11: 최종 회귀 + 배포 + Mac 워커 E2E

**Files:**
- 코드 변경 없음. 검증 + 실행.

- [ ] **Step 1: 전체 회귀**

```bash
cd /Users/seominjae/Documents/hydra
.venv/bin/pytest tests/ --ignore=tests/test_language_setup.py -q
```
415+ passed 예상.

- [ ] **Step 2: 프론트 빌드**

```bash
cd frontend && npm run build
```
성공.

- [ ] **Step 3: push + 어드민 UI 배포 버튼**

```bash
cd /Users/seominjae/Documents/hydra
git push origin main
```

어드민 UI `/` 에서 "배포" 버튼 클릭 (또는 API):
```bash
TOKEN=$(curl -s -X POST https://hydra-prod.duckdns.org/api/admin/auth/login \
    -H 'Content-Type: application/json' \
    -d "{\"email\":\"admin@hydra.local\",\"password\":\"$ADMIN_PASSWORD\"}" \
    | python3 -c 'import json,sys;print(json.load(sys.stdin)["token"])')
curl -s -X POST -H "Authorization: Bearer $TOKEN" \
    https://hydra-prod.duckdns.org/api/admin/deploy
```
25초 후 버전 업데이트 확인.

- [ ] **Step 4: Mac 워커 기동**

별도 터미널:
```bash
export ADMIN_EMAIL=admin@hydra.local
export ADMIN_PASSWORD='FgcrwvWv!RMYq35IKDPu'
bash scripts/run_mac_worker_dry.sh
```
로그에서 heartbeat 정상 출력 확인.

- [ ] **Step 5: 브라우저 관찰**

브라우저 `http://localhost:5175/workers` → 3초 내 "mac-dryrun" 워커가 온라인으로 표시.

어드민 UI `/accounts` → "계정 등록" → 임의 값 입력 → 등록.

`/tasks` → stats 카드에서 pending → running → done 수초 안에 변화 관찰.

`/workers` → 워커 카드의 current_task 필드가 실행 중 task 로 표시 → 완료 후 사라짐.

약 30~45초 내에 등록한 계정이 active 로 전이 (워밍업 3일 연속 → 졸업) → stub 캠페인이 감지 → comment/like 태스크 생성 → 워커가 처리 → done.

- [ ] **Step 6: 자동 검증 스크립트**

```bash
HYDRA_URL=https://hydra-prod.duckdns.org \
ADMIN_EMAIL=admin@hydra.local \
ADMIN_PASSWORD='...' \
bash scripts/m2_1_loop_check.sh
```

- [ ] **Step 7: e2e_check.sh 재실행**

```bash
HYDRA_URL=https://hydra-prod.duckdns.org \
ADMIN_EMAIL=admin@hydra.local \
ADMIN_PASSWORD='...' \
QUIET=1 bash scripts/e2e_check.sh
```
PASS 16/16 예상 (기존 14 + stats + recent).

---

## 완료 기준

1. `pytest` 415+ GREEN
2. 프론트 빌드 성공
3. Mac 워커가 `HYDRA_WORKER_DRY_RUN=1` 로 기동 → heartbeat 정상
4. 어드민 UI `/workers` 에 Mac 워커 "online" 3초 내 반영
5. 계정 등록 → 45초 내 `active` 도달 → stub 캠페인 comment/like 자동 처리
6. `/tasks` stats 카드 실시간 변화
7. `/workers` 카드 current_task 실시간 표시
8. e2e_check.sh 16 PASS

## 이후 (M2.2)

실 Gmail 1개 준비 → `HYDRA_WORKER_DRY_RUN=0` (또는 env 제거) 로 Mac 워커 재기동 → 실제 온보딩/워밍업 액션 검증. 여기서 드러나는 M1-12 concerns (OTP 저장 경로, identity_challenge 전이) 처리.
